<!DOCTYPE html>
<html>
<head>
  <title>Top Searches</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 20px;
    }
    h1 {
      font-size: 24px;
      margin-bottom: 10px;
    }
    ul {
      list-style-type: none;
      padding: 0;
    }
    li {
      margin: 5px 0;
      padding: 10px;
      border: 1px solid #ccc;
      border-radius: 4px;
    }
    .back-link {
      margin-top: 20px;
      display: block;
    }
  </style>
</head>
<body>
  <h1>Top Searches</h1>
  <ul>
    <% @top_searches.each do |query, count| %>
      <li><%= query %> - <%= count %> searches</li>
    <% end %>
  </ul>

  <%= link_to "Back to Search", root_path, class: "back-link" %>
</body>
</html>
